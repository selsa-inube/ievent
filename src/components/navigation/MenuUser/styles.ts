import styled from "styled-components";

const StyledAvatarContainer = styled.div`
  figure {
    width: 32px;
    height: 32px;
  }

  svg{
    margin: 2px;
  }
`;

export { StyledAvatarContainer };
